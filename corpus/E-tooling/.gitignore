# Mirrored third-party repos. Regenerate with:
#   cd corpus/E-tooling && ./runbatch.sh 1 100          (targets.txt)
#   cd corpus/E-tooling && TF=targets2.txt ./runbatch.sh 1 20
# Every clone's exact HEAD sha is pinned in TOOLS.json, so these are re-derivable.
vendor/
clone.log
