# Round 27 — Oracle adjudication: S1 claim-bar crosser, seed 35563892

**Date:** 2026-09-07 · **Adjudicator of record:** `round19/I2/adjudicate.py` via
`round20/HITFN/hitfn20.py`, driven by `oracle_35563892.py` (this dir), which imports the
R25 reference pipeline verbatim through `P2-harness/pyref.py`. Sweep untouched (ran
`nice -n 15` in this dir only; the engine kept grinding).

**Context.** First C-screen claim-bar crosser of the round-27 S1 lane (relation `pair`/
keyskip2, Py2.7-MT 2^32 word space, LP2 unsolved head). C screen reported pmax
**7.6707232992600876** vs the S1 claim bar **7.3835202943286884**
(`P1-engine/run/HIT-CANDIDATE.json`). The bar is a 1e6-seed alpha=0.01 Gumbel bar, so over
a 4.3e9-seed lane ~tens of noise crossers are *expected*; doctrine (R21-L1) still demands
full adjudication — FLAGGED-FOR-ORACLE, never auto-certified, never waved off.

## 1. C vs Python parity (K4 check)

| | pmax |
|---|---|
| C screen (grind27, lane S1) | 7.6707232992600876 |
| Python reference (`runner.stage_a` path, L=120) | 7.6707232992600876 |
| **abs delta** | **0.0** (tolerance 1e-6; K4 not triggered — parity holds exactly) |

Stage-A argmax register: **LP1_REAL** (z = 7.671); full panel z =
[4.87, 6.05, 7.67, 3.57, 4.04, 2.36, 3.07, 4.66, −0.34]; en −6.652, ioc·N 1.170, mds 15,
h2 0.176 bits, zl 0.817.

## 2. hitfn20 three-clause gate (L=240 hit window, N=1e6, alpha=0.01)

| clause | requirement | measured | result |
|---|---|---|---|
| 1 — panel-max null | pmax >= 7.383520294328688 | **4.963841996903331** | **FAIL** |
| 2 — rune-index recovery | >= 0.90 (source: heldout_selfconsistency) | 1.0000 | pass |
| 3 — held-out 3/4 reproduction | >= 0.90 under the same key | 1.0000 | pass |

**hit = False.** Gate reason of record: `FAIL clause1: pmax 4.964 < panel-max bar 7.384`.

On clauses 2/3 passing at 1.0: for a real candidate (no truth_idx) both clauses measure
whether the decode *reproduces itself* off the attributed key phase — a deterministic
decode of noise reproduces perfectly, so 1.0 here carries no language evidence; the gate is
designed to catch hallucinating beams, and this decode isn't hallucinating, it's honestly
decoding noise. The informative clause is 1, and it fails by **2.42 z** — not marginal.

The failure mode is the canonical noise-crosser signature: the 120-rune screen fluctuation
(7.67) does not extend when the window doubles — at L=240 the same seed/register drops to
4.96 (stage-b panel z = [3.72, 4.38, 4.96, 2.78, 2.32, 1.04, 2.74, 2.80, −0.95];
ioc·N 1.029 ≈ flat, h2 0.265 bits ≈ the wrong-key null 0.262, mds 15). A true key gains
evidence with length; noise regresses to the mean.

## 3. Decoded output (register fired: LP1_REAL — the rune-trigram model of real LP1 pages, not English)

First 200 transliterated chars of the 240-rune hit-window decode:

```
AGHPOPOGPDGMUTHDXGXBEAFGYANTTOMJBETTLORAEAUPXLIABAEXHNGYOURNGNTNTHTHFROLLDMEOTGNNEAATDIESFODAFRILCMXPATINGTHINGAEDTTOEIAICTUNCEODURALMTIAIAJSAEAEPEALPPEOCDEOLXXXGRFAEARJWGJYARIRJDGXDIAGBOLHFBOEUOUEOCU
```

Not language. Chance English-looking fragments ("YOUR", "THING", "DIES") at the rate any
29-symbol stream produces them; no morphology, no word boundaries, en quadgram −6.65
(deep noise territory). Consistent with LP1_REAL firing on rune-frequency texture rather
than content.

## Verdict

**NOISE-CROSSER** — parity exact (delta 0.0), gate FAIL on clause 1 (pmax 4.964 < bar
7.384 at L=240); the seed is adjudicated dead and the S1 lane's null stands. The sweep was
not stopped and continues.
