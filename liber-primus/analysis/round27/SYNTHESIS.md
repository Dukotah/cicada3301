# Round 27 — SYNTHESIS: the C port fires the full 2³² derived-key sweep

_Written 2026-09-07 at sweep launch (round remains **in-flight**: the sweep completes
~2026-09-08). Trust anchor `python3 tests/validate.py` → **ALL VALIDATIONS PASSED (5/5)**
before the build, before the launch, and at close-out. `validate_ledger.py`
**Unsound negatives = 0**. Ledger **156 → 157** (R27-CPORT-MT32-FULLSWEEP, in-flight)._

## The question this round asked

After 26 rounds the standing verdict is **LP2 0–54 is OTP-class**, and the ONLY
internally-runnable verdict-changer left is finishing the finite derived-key branch:
the Python-2.7 Mersenne-Twister `init_by_array([w])` 2³² seed space under the skip-aware
decoder. Round 25 ground it in pure Python and parked at **0.5015%** coverage
(21,539,647 words, 0 hits, best pmax 6.826 vs claim bar 7.384, ~1650–1966 seeds/s —
~25–32 wall-days remaining). Round 27's whole job was the **~65× speedup that converts
that branch from parked to finishable**: a bit-exact C port of the R25 stage-A screen,
gated so hard it is *provably incapable of throwing the true key away* (the R18
"broken magnet" failure, excluded structurally this time), then fire the full space.

Three build lanes + one red-team lane, all under `PREREG.md` (five Aiming-Test answers,
frozen thresholds, kill conditions K1–K4):

- **P0 — spec + frozen truth**: `P0-spec/SPEC.md` and `vectors.json` (64 reference
  vectors + planted controls), generated by `make_vectors.py` **from the R25 pipeline
  verbatim** (imports `round25/compute-tail/runner.py`; reimplements nothing). The plant
  existed, in the actual shape, before any C code did (doctrine Q1).
- **P1 — the engine**: `grind27.c` (gcc -O3 -fopenmp, `-ffp-contract=off` for FP parity;
  no `-ffast-math`, it breaks bit-parity). Its ONLY filter is the candidate bar 5.0 —
  2.38 below the claim bar and *below the empirical null maximum itself* — and every
  seed at/above it is appended to `candidates.jsonl` for **Python to adjudicate**. The
  engine makes no hit decision, computes no recovery, certifies nothing (SPEC §9).
- **P2 — the acceptance harness**: `ENGINE-CONTRACT.md` + `run_all.sh` (check_vectors,
  check_drill, check_false_reject, check_r25_repro, bench). The gate FAILed the original
  CLI once and P1 had to conform — the harness is real, not decorative.
- **R — red-team**, refute-by-default, over the GO gate itself.

## The gate — the instrument's power is now MEASURED, not argued (coverage × power)

Doctrine R1/R2: no null counts until the instrument demonstrably recognises the thing
being hunted. Every clause below was measured before the sweep was allowed to start:

| gate clause | measured result |
|---|---|
| **V1 parity** | 64/64 reference vectors bit-exact vs the R25 Python pipeline, worst \|Δpmax\| = **0.0** |
| **V2 planted screen** | seed-777 skip_by_two plant scores pmax **17.931158509762** vs cand bar 5.0 — margin **12.93**; the screen cannot reject a true seed of this class |
| **V3 end-to-end drill** | band sweep recovers 2149309687 → **6.825835461843649** (the R25 global best), \|d\| = 0 |
| **False-reject, 0 observed** | 100k seeds from frozen RNG 27002: Python max 5.538506 @ 342028379, 0 ≥ hard gate 5.8835, 6 ≥ cand bar 5.0 — **all 6 C-flagged, \|d\| = 0** → false-reject **0/6** |
| **K4 parity in production** | 95/95 C-flagged candidates re-scored by `runner.stage_a`: **zero** relative difference |
| **Full-gate plant (pre-C)** | seed 777 full construct: recovery 1.000, held-out 1.000, pmax 25.239 ≥ 7.384 → **HIT=True** (`vectors.json["planted_selftest"]`) |
| **Throughput gate** | **128,529 seeds/s** aggregate (harness bench; 158k/s burst, 104k/s sustained — the host thermally throttles; environment, not engine). vs Python's ~1800/s: ~**70×** |

**Verdict: GO.** Power statement for the eventual null: screen power = plant margin 12.9
over the candidate bar with 0 measured false-rejects at parity \|d\|=0; claim power =
hitfn20 3-clause on a HIT=True-validated plant. Coverage will be **1.0 per cell by
construction** (clean [0, 2³²) cursors, no sampling, no exclusion-set bookkeeping —
R25's 0.5015% is deliberately re-covered for ~10–20 min so "no seed was ever skipped"
is trivially auditable).

## The sweep — plan and live status (in-flight at close-out)

`sweep_plan.json`, executed sequentially by `./grind27 --plan ../sweep_plan.json
--run-dir run` (detached via `setsid`, survives session exit; K3 planted self-test at
every launch; SIGTERM checkpoints cleanly; resume proven exact — no gap, no double-count):

- **S1 — pair/keyskip2, the exact R25 configuration, full 2³² from 0.** Claim bar
  `panelmax_bar("pair", 1e6, 0.01)` = **7.383520294328688**. ETA ~9.3 h at the gate
  figure, ~11.3 h at the throttled-sustained ~106k/s (0.82× gate — inside the required
  2× band), completing ~**2026-09-08 06:30 local**.
- **S2 — keyskip1 'exact', same space/reducer/offset.** Claim bar **7.6341931878728095**.
  Same duration again; S1+S2 ≈ **22.5 h total**. **Gated:** S2's null does NOT count
  until its own planted control + V1-style vector file (encipher_keyskip plant) pass —
  the binary refuses to start S2 without `GRIND27_S2_CONTROL_OK=1`.
- **S3 cells deferred** (other reducers × offsets): completeness rituals with no
  per-cell controls built yet; adding them now would generate unvalidated nulls.

**Live at close-out (2026-09-07 ~19:36, ~18 min into the current run):** S1 at
**2.46%** (105.5M seeds), throughput ~100.2k seeds/s, **0 claim-bar hits**, 9,677
candidates (9.2e−5/seed ≈ 1.3× the Gumbel prediction 6.95e−5 → **K2 passed live**),
best pmax **7.035050 @ word 2873275872** — 0.349 *below* the claim bar, sitting in
`candidates.jsonl` awaiting Python stage_b re-score like everything else. All 6 workers
alive, checkpoints atomic every ~30 s. Ops runbook: `MONITORING.md` (status command,
resume recipe, what `HIT-CANDIDATE.json` means — FLAGGED-FOR-ORACLE, never
auto-certified, per R21-L1).

## Red-team (Lane R) — NO-ERROR-FOUND on the GO gate

`R-redteam/RESULTS.md` (+ `audit1.py`, `audit2.py`, `miniplan.json`, `testrun/` — all
reproducible). Recomputed **every** gate number from raw artifacts: the claim bar
7.383520294328688 re-derives from the Gumbel cell exactly; the `py_scores.jsonl` seed
set regenerates from frozen RNG 27002; the Python-side max/candidate counts reproduce;
all 6 near-bar seeds re-score against the real binary at \|d\| = 0; `engine.dat` is
byte-equal to `vectors.json`'s constants; `make_vectors.py` verifiably imports the R25
pipeline rather than reimplementing it (no closed loop). The broken-magnet hazard was
attacked three fresh ways and held: (a) source-verified that the claim-bar comparison
only ADDS a flag file and can suppress nothing; (b) 35 fresh adversarial seeds incl. a
**novel plant at 271828182** no prior artifact ever used — worst \|pmax diff\| = 0,
plant recovery 1.000, neighbouring wrong seeds score 0.33–1.99; (c) the REAL production
path (`run_lane`) interrupted mid-band at 11% and resumed — final coverage exactly
100,000/100,000, candidates bit-identical to the live run's flags. Five NOTE-severity
findings, none invalidating GO; the operational one (**N4**: no concurrent Python
stage_b consumer running yet, so K4 only has teeth at batch time) is carried into the
ledger row's `not_covered`.

## Honest scope — what this round's eventual null will and will not mean

The three conditionals (doctrine Q4), restated: **(1) key space** = Py2.7/mt19937ar
`init_by_array([w])`, w ∈ [0, 2³²), reducer `random29`, offset 0 — NOT multi-word keys,
string seeds (hashed path, swept separately R19–R24), jumpahead states, or other
generators; **(2) decoder transition model** = driftbeam keyskip2 'pair' (S1) /
keyskip1 'exact' (S2, control-gated) — rejection-loop constructions only; **(3)
adjudicator register** = the I2 9-register panel-max + English quadgrams at L120, hitfn20
3-clause at claim — a plaintext outside those registers can still be missed (L7-A
generalises; both plants use English, red-team N5). This round covers **the enumerated
Py2.7-MT cells ONLY. The `/dev/urandom` branch is untouched by construction and no
amount of compute here reaches it.** **The standing OTP-class verdict is UNCHANGED and
stays unchanged until the sweep completes and its candidates are Python-adjudicated** —
at which point the result is either a FLAGGED-FOR-ORACLE survivor or the conversion of
"probably not a cheap seeded PRNG" into a **measured exhaustion of the cell**, which is
bounds, not a verdict.

## Bottom line (honest, one paragraph)

Round 27 built and *proved* the instrument Rounds 21/25 said this branch needed — a
~70× C port whose parity with the Python adjudicator is measured at \|d\| = 0 across
vectors, plants, false-reject samples, fresh adversarial seeds and the live production
path, whose only filter sits below the empirical null maximum (it can over-flag but
structurally cannot under-flag), and whose GO gate survived a refute-by-default red-team
at NO-ERROR-FOUND — then fired the full 2³² space on both representable rejection-loop
relations. **0 claim-bar hits so far at 2.46% of S1; candidate rate exactly where the
null theory puts it.** Nothing is claimed closed today: the round is in-flight, S2's
null is control-gated, and the finish line is a Python-adjudicated candidate set, not a
C counter reaching 2³². If the true key is a 32-bit Py2.7 seed under these relations,
this engine will flag it; if the sweep comes back empty, the last internally-runnable
verdict-changer becomes a measured bound and the doors that remain are the ones that
were always there — an external key surfacing, or a pad this instrument was never going
to see.
