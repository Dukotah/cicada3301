# Round 27 / S2 (keyskip1 'exact') — planted control + reference vectors: NULL ARMED

**Date:** 2026-09-07. **Status: ALL CHECKS PASS — the S2 null is armed.**
This delivers the artifact the PREREG explicitly gates S2 on ("Requires its own planted
control (encipher_keyskip plant, not skip_by_two) and its own V1-style vector file
before it runs") and that `sweep_plan.json`'s S2 note assigns to the P0/P2 lanes.
Doctrine (`ARMADA-DOCTRINE.md` §4.2): a null from an unvalidated instrument is not a
negative — plant, prove recovery, then trust silence.

Everything below was run WITHOUT touching the live S1 sweep (PID in `../sweep.pid`,
run dir `P1-engine/run/` — never written to; engine invocations used separate run dirs
under `P2-harness/s2check/`, `nice -n 15`, `--threads 1`).

## Artifacts

| file | what |
|---|---|
| `P0-spec/vectors_s2.json` | S2 V1-style reference vectors: 64 seeds through the Python reference pipeline (R25 `runner.py` imported verbatim) under `DB.PRESETS["exact"]` (mode keyskip1, max_skip=3) on the real C_SCREEN; S2 constants (claim bar + Gumbel cell); the S2 planted control (truth, keystream head, cipher, verdict) |
| `P0-spec/make_vectors_s2.py` | generates it (same pattern as `make_vectors.py`; reimplements nothing) |
| `P2-harness/s2check/check_s2.py` | the acceptance checks against the DEPLOYED, UNMODIFIED `P1-engine/grind27` binary, through its real sweep code path (`--plan` → `run_lane` → `decode_seed` with pair=0) |

Same 64-seed list as S1's `vectors.json` (FIXED_SEEDS + `Random(20260907)`) on purpose:
ks128 parity for these seeds is already established by the S1 V1 gate (the keystream
does not depend on the relation), so the S2 checks isolate the relation change.
Confirmed the relation actually changes the decode: S2 pmax differs from S1 pmax on
62/64 seeds (the 2 coincidences are skip-free best paths, identical under both modes).

## Check 1 — S2 vector parity (PASS)

The Python reference: `beam_decode(C_SCREEN, K, sign=-1, o=0, beam_w=64, mode="keyskip1",
max_skip=3, max_free=0, lam=0.0, start_slack=0)` + `adjudicate(...)["pmax"]`, for all 64
seeds. Stage-A pmax range on these (noise) seeds: [−0.677, 3.290] — all far below the
claim bar, as expected.

C side: a plan of 64 width-1 `relation:"exact"` lanes (one per seed) through the real
`--plan` sweep path; each lane's `top_<lane>.json` records that seed's pmax at `%.17g`
(this channel works below the candidate bar and for negative pmax).

**Result: 64/64, worst relative difference 0.0** (bit-exact to the printed 17 digits;
the task bar was ≤ 1e−6 rel). `ptr_end`/`n_skips`/`nchars`/`plain_idx` per seed are
frozen in `vectors_s2.json` for any future stricter check.

## Check 2 — planted control through the real C sweep path (PASS)

**Construction** (the one keyskip1 is exact for — NOT S1's skip_by_two):
`data/keys/self_reliance.txt` → `eng_to_idx[5000:5240]` (240 runes), enciphered by
`campaign18_skip/skipdecode.encipher_keyskip(P, K, sign=-1, supp=0.83, seed=777)` —
the ONE-draw rejection loop (reject a doubling key symbol with p=0.83, advance j by 1)
— with keystream = `word_stream(777, 1504)` (random29). The plant contains 9 key skips
and differs from S1's skip_by_two plant cipher at 166/240 positions. (Identical truth
plaintext ⇒ the recovered stage-A pmax value legitimately matches S1's plant.)

**C run:** `engine_plant.dat` = the deployed `engine.dat` with CSCREEN := plant
cipher[:120] and the 64 launch-self-test VEC entries regenerated by the Python pipeline
for that cipher (so the launch V1+V2 self-test stayed a REAL check — it PASSed, not
bypassed). Single lane `S2PLANT`, relation `exact`, band **[745, 809)** — seed 777
mid-band — swept by the deployed binary's real `run_lane` path into
`s2check/plant_run/`.

**Result:**

- seed **777 flagged into `candidates.jsonl`** with pmax **17.93115850976196** —
  C vs Python absolute difference **0.0** (bar ≤ 1e−9);
- clears the candidate bar 5.0 by **12.93** and the **S2 claim bar 7.6341931878728095
  by 10.30** ⇒ `HIT-CANDIDATE.json` raised (FLAGGED-FOR-ORACLE semantics observed:
  the sweep continued);
- no other seed in the band was flagged (0 false positives at cand_bar 5.0);
- the flagged seed was then adjudicated by the REAL Python 3-clause gate
  (`hitfn20.evaluate`, preset="exact", N=1e6, strict `truth_idx`):
  **HIT=True, recovery 1.000, held-out 1.000, pmax 25.239 vs bar 7.634**.

So the S2 screen is provably incapable of rejecting a true seed of this
construction/register class, and the full gate certifies recovery ≥ 0.90 on it.

## Constants verified

- `panelmax_bar("exact", 1e6, 0.01) = 7.6341931878728095` — matches `sweep_plan.json`
  S2 and `engine.dat` `CLAIM_BAR_EXACT` exactly (asserted in `make_vectors_s2.py`);
  cell `I19:vecbeam.keyskip1+I2|pmax|L120` (contract frozen in `vectors_s2.json`).
- Panel mu/sd at n=120, quadgram total/floor: identical to S1 (relation-independent).

## Exact commands (reproduce end-to-end)

```bash
cd liber-primus/analysis/round27/P0-spec && nice -n 15 python3 make_vectors_s2.py
cd ../P2-harness/s2check && nice -n 15 python3 check_s2.py     # OVERALL: PASS, rc 0
```

`check_s2.py` sets `GRIND27_S2_CONTROL_OK=1` for its own child engine processes only —
that override exists precisely for "the control is validated"; this run IS that
validation, and this document is the record that arms it.

## Operational note (for whoever tends the live sweep)

The live sweeper (launched before this control existed) does NOT have
`GRIND27_S2_CONTROL_OK=1` in its environment. When it finishes S1 and reaches the S2
lane it will print `REFUSING lane S2 …` and exit — **that is the gate working, not a
crash.** Resume per `MONITORING.md`'s recipe with the variable set:

```bash
cd liber-primus/analysis/round27/P1-engine && \
GRIND27_S2_CONTROL_OK=1 setsid nice -n 10 nohup ./grind27 --plan ../sweep_plan.json \
  --run-dir run >> ../sweep.log 2>&1 & sleep 2; pgrep -x grind27 | tee ../sweep.pid
```

(S1 will be in `lanes_completed` and is skipped; resume is proven exact.)

## What the S2 null will mean (and not mean)

Armed null, conditional on exactly (PREREG Q4): key space = Py2.7 MT
`init_by_array([w])`, w ∈ [0, 2^32), reducer random29, offset 0; transition model =
keyskip1 'exact' (the one-draw rejection loop; NOT constructions outside
keyskip1/keyskip2/permissive representability); register = the 9-register panel.
Power: screen plant margin 12.93 over cand_bar / 10.30 over claim bar; full-gate plant
recovery 1.000. Coverage: to be reported by the sweep itself.
