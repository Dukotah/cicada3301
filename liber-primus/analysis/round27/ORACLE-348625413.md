# Round 27 — Oracle adjudication: S1 claim-bar crosser, seed 348625413

**Date:** 2026-09-08 · **Adjudicator of record:** `round19/I2/adjudicate.py` via
`round20/HITFN/hitfn20.py`, driven by `oracle_crosser.py` (this dir, the parameterized
adaptation of `oracle_35563892.py`), which imports the R25 reference pipeline verbatim
through `P2-harness/pyref.py`. Sweep untouched (S2 kept grinding; this ran `nice -n 15`
single-threaded).

**Context.** Second C-screen claim-bar crosser of the round-27 S1 lane (relation `pair`/
keyskip2, Py2.7-MT 2^32 word space, LP2 unsolved head), logged in
`P1-engine/run/HIT-CANDIDATE.json` after lane resume. C screen reported pmax
**7.6001248907122188** vs the S1 claim bar **7.3835202943286884**. The bar is a
1e6-seed alpha=0.01 Gumbel bar, so over a 4.3e9-seed lane ~tens of noise crossers are
*expected*; doctrine (R21-L1) still demands full adjudication — FLAGGED-FOR-ORACLE,
never auto-certified, never waved off.

## 1. C vs Python parity (K4 check)

| | pmax |
|---|---|
| C screen (grind27, lane S1) | 7.600124890712219 |
| Python reference (`runner.stage_a` path, L=120) | 7.600124890712219 |
| **abs delta** | **0.0** (tolerance 1e-6; K4 not triggered — parity holds exactly) |

Stage-A argmax register: **LP1_REAL** (z = 7.600); full panel z =
[3.82, 5.84, 7.60, 1.60, 3.28, 2.91, 2.58, 2.94, 0.67]; en −6.414, ioc·N 1.283, mds 12,
h2 0.199 bits, zl 0.817.

## 2. hitfn20 three-clause gate (L=240 hit window, N=1e6, alpha=0.01)

| clause | requirement | measured | result |
|---|---|---|---|
| 1 — panel-max null | pmax >= 7.383520294328688 | **4.533995152761554** | **FAIL** |
| 2 — rune-index recovery | >= 0.90 (source: heldout_selfconsistency) | 1.0000 | pass |
| 3 — held-out 3/4 reproduction | >= 0.90 under the same key | 1.0000 | pass |

**hit = False.** Gate reason of record: `FAIL clause1: pmax 4.534 < panel-max bar 7.384`.

Clauses 2/3 at 1.0 carry no language evidence for a real candidate (no truth_idx): a
deterministic decode of noise reproduces itself perfectly; those clauses exist to catch
hallucinating beams. The informative clause is 1, and it fails by **3.07 z** — worse
than the first crosser (35563892 failed by 2.42 z), not marginal.

The failure mode is the canonical noise-crosser signature: the 120-rune screen
fluctuation (7.60) does not extend when the window doubles — at L=240 the same
seed/register drops to 4.53 (stage-b panel z = [3.35, 3.43, 4.53, 2.56, 2.13, 3.33,
2.24, 1.93, 0.45]; ioc·N 1.135, h2 0.287 bits ≈ the wrong-key null, mds 12). A true key
gains evidence with length; noise regresses to the mean.

## 3. Decoded output (register fired: LP1_REAL — the rune-trigram model of real LP1 pages, not English)

First 200 transliterated chars of the 240-rune hit-window decode:

```
GTXAELRJEATHGOTATISTHEOTHEOISETEEDREOTHXEAXDECEIAHTEOLOEYACSTLEAGELIYXFIAEOFOEIAXRTPRIDNFLRIALPOLTSACPAENGUIAGPOEIASIANIACEAARAENGIRTHUETTIAGEONGNGEAGIANGMXOUEATHYLIATHIAEAHTAEBCDUJIXEPAEDTIPGMFROAEYO
```

Not language. Chance English-looking fragments ("ISTHEOTHE", "PRID", "GOT") at the rate
any 29-symbol stream produces them; no morphology, no word boundaries, en quadgram −6.41
(deep noise territory). Consistent with LP1_REAL firing on rune-frequency texture rather
than content.

## Verdict

**NOISE-CROSSER** — parity exact (delta 0.0), gate FAIL on clause 1 (pmax 4.534 < bar
7.384 at L=240); the seed is adjudicated dead and the S1 lane's null stands.
